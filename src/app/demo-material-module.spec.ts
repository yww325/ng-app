import { DemoMaterialModule } from './demo-material-module';

describe('DemoMaterialModule', () => {
  it('should create an instance', () => {
    expect(new DemoMaterialModule()).toBeTruthy();
  });
});
